model Message {
    id         String @id @default(uuid())
    message    String
    sender     User   @relation(name: "send_messages", fields: [senderId], references: [sub])
    senderId   String
    receiver   User   @relation(name: "received_messages", fields: [receiverId], references: [sub])
    receiverId String

    @@map("messages")
}
